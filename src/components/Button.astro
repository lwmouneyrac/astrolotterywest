---
const { url, label } = Astro.props;
---

<a
    href={url}
    class={`inline-flex items-center justify-center transition-all duration-500 border border-primary rounded bg-white cursor-pointer px-5 py-2 min-w-[100px] min-h-[33px] text-center no-underline leading-tight font-poppins font-bold text-primary m-5 text-sm hover:bg-primary hover:text-white`}
    >{label}</a
>
